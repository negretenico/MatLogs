import { Color } from '../types/Color';

export const categoryMapper: Record<string, Color> = {
  Escape: 'blue', // Represents calmness and focus, fitting for evasion.
  Sweep: 'yellow', // Bright and swift, conveying the idea of quick movement.
  Pass: 'green', // Suggests progress and success, like "go."
  Takedown: 'red', // Intense and aggressive, fitting for an offensive action.
  Guard: 'gray', // Neutral and defensive, representing stability.
  Attack: 'indigo', // Deep and intense, suggesting a calculated offensive move.
};
